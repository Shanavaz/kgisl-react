// require('dotenv').config();
require('dotenv');

const configDev = {
    server: {
        url: 'localhost',
        port: ':3001',
        contextpath: '',
        serverUrl: 'localhost:3001',
        protocol: 'http://'
    }
};

module.exports = configDev;
